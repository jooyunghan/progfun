package forcomp

import Anagrams._

object exercise {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(80); val res$0 = 
List.range(1,10) map (_*2);System.out.println("""res0: List[Int] = """ + $show(res$0))}
	
	
	
}
